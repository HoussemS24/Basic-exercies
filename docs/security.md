# Security Policy
- No plaintext secrets in repo.
- App keys and refresh tokens in OS keychain.
- Local caches encrypted using APP_ENCRYPTION_KEY.
- Logs are structured and redacted.
- Telemetry/crash is opt-in and scrubbed.
